# Local Best Submission Assets

This directory is intentionally public-code-only.

Private best-attempt artifacts are ignored:

- `manifest.json`
- `submission-best.zip`
- `onnx/*.onnx`

To enable the local Best Template panel, place your private manifest and ONNX files here on your own machine. Do not commit competition submission artifacts to the public repo.
